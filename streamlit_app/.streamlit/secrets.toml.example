# In the DuckDB-in-repo edition the Streamlit app needs no secrets — the
# data lives in `data/eqdp.duckdb`, which is committed to the repo and
# pulled down on deploy. You can leave secrets blank on Streamlit Cloud.
#
# This file is kept as a placeholder for any future optional config:
#
# ADMIN_EMAIL = "palani@unisongroup.com"
