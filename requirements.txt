# sg-eqdp-scanner — Python dependencies
# Pin versions; bump deliberately, not accidentally.

# Data
pandas==2.2.3
numpy==2.1.3
duckdb>=1.5.0,<2.0    # 1.1.3 has a bug where DEFAULT uuid() fires on UPDATE
pyarrow==18.1.0
yfinance>=1.3.0        # 0.2.x is broken against Yahoo's new bot-detection.
curl_cffi>=0.7         # required by yfinance for browser-TLS impersonation;
                       # without it Yahoo silently rejects SGX requests.

# Statistics / econometrics
scipy==1.14.1
statsmodels==0.14.4
linearmodels==6.1
# Bayesian synthetic control. Deferred (Day 5) — tfcausalimpact 0.0.16 is
# Python<3.12, and 0.0.18 pins pandas<=2.2 which clashes with our 2.2.3.
# When we wire up synth-control we'll either re-pin pandas or use a hand-rolled
# Abadie estimator. For now, the four event studies + DiD + bootstrap pipelines
# don't need it.
# tfcausalimpact==0.0.18

# HTTP / scraping
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0
tenacity==9.0.0          # retries with backoff

# Database — DuckDB-in-repo (the file lives at data/eqdp.duckdb).
# Supabase is no longer used. duckdb is also pinned above for analytical work.

# Plotting (Streamlit app uses plotly; matplotlib for static PDF exports)
matplotlib==3.9.3
plotly==5.24.1

# Streamlit web app (replaces the legacy web/ Next.js app)
streamlit==1.40.2
streamlit-extras==0.5.0
# RSI / MACD / Bollinger / ATR are implemented in streamlit_app/lib/ta.py from
# first principles — no pandas-ta to avoid pkg_resources/numpy quirks on Cloud.

# Utilities
python-dotenv==1.0.1
pydantic==2.10.3
typer==0.15.1            # tiny CLI for pipelines
rich==13.9.4             # nicer logs

# Testing / dev
pytest==8.3.4
pytest-cov==6.0.0
mypy==1.13.0
ruff==0.8.4
