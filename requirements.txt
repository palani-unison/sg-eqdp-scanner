# sg-eqdp-scanner — Streamlit Cloud runtime dependencies (slim).
#
# This file is what Streamlit Community Cloud installs on every cold start.
# Keep it MINIMAL. Anything the streamlit_app/ code does not import directly
# belongs in requirements-dev.txt (pipelines, tests, lint).
#
# IMPORTANT — Streamlit Cloud Python version:
#   Set Python to 3.12 in the app's Advanced Settings on the dashboard.
#   The pinned versions below have prebuilt wheels for 3.12 but not for
#   3.13 / 3.14, so the default Streamlit Cloud Python will source-build
#   and may fail (lxml / scipy need C compilers).

streamlit==1.40.2
pandas==2.2.3
numpy==2.1.3
duckdb>=1.5.0,<2.0
plotly==5.24.1
